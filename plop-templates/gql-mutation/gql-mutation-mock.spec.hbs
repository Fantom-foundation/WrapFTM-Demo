import { {{ camelCase name }}Mock } from './{{ name }}.js';

describe('{{ camelCase name }}Mock() mock', () => {
    it('should return expected outcome', async () => {
        const result = {{ camelCase name }}Mock().data;

        expect(result).toEqual('');
    });
});
